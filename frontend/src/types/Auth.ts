export type LoginT = {
  email: string;
  password: string;
};

export type UserT = {
  name: string;
  email: string;
};

export type SignupT = {
  name: string;
  email: string;
  password: string;
};
